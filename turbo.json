{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {},
    "build": {
      "outputs": [".next/**", "dist", "build"],
      "cache": true,
      "dependsOn": ["^build"]
    }
  }
}
